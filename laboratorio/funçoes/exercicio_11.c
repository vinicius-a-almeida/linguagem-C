#include <stdio.h>
#include <stdlib.h>

int area (int r);
int vol (int r);
int main (){
    int n1, ret_area, ret_vol;
    printf("digite o valor do raio: ");
    scanf("%d", &n1);
    ret_area = area (n1);
    ret_vol = vol (n1);
    system ("pause");
    return 0;
}
int area (int r){
    int 
}